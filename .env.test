PORT=3001

# Database de test (gardenmate_api postgres sur port 5434)
DB_HOST=localhost
DB_PORT=5434
EXTERNAL_DB_PORT=5434
DB_USER=garden
DB_PASSWORD=secret
DB_NAME=gardenmate_test

# CORS
CORS_ORIGIN=http://localhost:3001

# JWT (secrets légers — ne jamais utiliser en prod)
JWT_ACCESS_SECRET=test_jwt_access_secret_e2e
JWT_REFRESH_SECRET=test_jwt_refresh_secret_e2e
